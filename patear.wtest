import objetos.*
import wollok.game.*
describe "Testeando la pegada de Lionel"{
    method initialize(){
        game.width(15)
        game.height(10)
        game.clear()
    }
    test "La zurda de Leo"{
        lionel.position(game.at(5,5))
        pelota.position(game.at(5,5))
        lionel.patear()
        assert.equals(game.at(8,5), pelota.position())
    }
}